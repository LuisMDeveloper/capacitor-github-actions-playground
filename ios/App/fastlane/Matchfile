require_relative 'config'

# The URL of the git repository where the provisioning profiles and certificates will be stored
git_url(AppConfig::CERTIFICATES_REPO)

storage_mode("git")

type(AppConfig::MATCH_TYPE)

app_identifier([AppConfig::APP_IDENTIFIER]) # The bundle identifier of your app
username(AppConfig::APPLE_ID) # Apple ID
